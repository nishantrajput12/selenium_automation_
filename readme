step1: first install this first in your comand prompt 
"pip install selenium webdriver-manager"

step2: again type 
    pip install selenium

step 3: in command prompt type 
  python test_script.py